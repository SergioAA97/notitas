"use strict";

function createNote(_ref) {
  var title = _ref.title,
      body = _ref.body;

  var note = {};

  return note;
}

createNote("Titulo", "body");
createNote({ title: "Titulo", body: "Body" });